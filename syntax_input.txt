! Find largest value between two numbers!

int num1, num2, large;

float n1, n2, l;

bool nu1, nu2, la;

num1 = 2;

num2 = 3;

large = num1 + num2;

large = num2 - num1;

large = large * large;

large = num2 / num1;

if(large > num2){ large = num1;} else{  large = num2; }

if(num1 < num2){ large = num1;} else{ large = num2; }

while(num1 > num2) { large = num1;}
 
while(num1 < num2) { large = num2;}
